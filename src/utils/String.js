/**
 * Number
 * @author GJMA
 * @date 2019-02-18
 **/

/**
 * @author GJMA
 * @date 2019-02-18
 * 生成随机数
 */
export const trim = (str) => {
  let result = str.replace(/(^\s+)|(\s+$)/g, '')
  console.log(result)
  return str.replace(/(^\s+)|(\s+$)/g, '')
}

trim('dd ddds sf')
